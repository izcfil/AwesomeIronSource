    import XCTest
    @testable import AwesomeIronSource

    final class AwesomeIronSourceTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(AwesomeIronSource().text, "Hello, World!")
        }
    }
